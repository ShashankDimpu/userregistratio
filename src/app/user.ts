export interface user {
    //create a class based on the columns created in SQL
        id:number;
        Name:string;
        Gender:string;
        Email:string;
        Username:string;
        password:string;
}
